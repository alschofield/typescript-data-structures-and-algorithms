import { describe, it } from "vitest";
import type { GraphView } from "./graph-view";

describe("GraphView", () => {
  it.todo("covers vertexCount and deterministic weighted index neighbors");
  it.todo("covers index adapters for adjacency lists, adjacency matrices, and imported graphs");
});
